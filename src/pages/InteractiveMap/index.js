/**
 * index.js — entry point for the Interactive Map page. Re-exports
 * the page component so the App.js router can import it from
 * "./pages/InteractiveMap" (no extension, no deep path).
 */
export { default } from "./InteractiveMapPage";
